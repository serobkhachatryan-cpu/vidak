/**
 * Shared Web3 Adapter entity kinds.
 * Kept free of Drizzle/runtime imports so server-config can reference them.
 */

export type W3dsAdapterEntityType = 'profile' | 'channel' | 'video' | 'playlist' | 'comment';
